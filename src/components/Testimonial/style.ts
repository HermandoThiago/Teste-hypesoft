import styled from 'styled-components';
import { StyledBox } from '../Box/style';

export const StyledContainerTestimonial = styled(StyledBox)`
    padding: 0 20px;
    
    @media screen and (max-width: 768px){
        div{
            width: 100%;
            margin: 0 auto;
        }

    }
`;

export const StyledContainerComment = styled(StyledBox)`

`;
