import styled from 'styled-components';

export const StyledHeader = styled.div`
    width: 100%;
    display: flex;
    flex-wrap: wrap;
    flex-direction: row-reverse;
    
    .nav{
        padding: 25px 0px;
        width: 36%;
        display: flex;
        flex-wrap: wrap;
    }

    p{
        margin-left: 15px;
        font-weight: 500;
        cursor: pointer;
    }

`;
